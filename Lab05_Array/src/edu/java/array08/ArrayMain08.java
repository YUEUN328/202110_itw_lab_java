package edu.java.array08;

import java.util.Random;

public class ArrayMain08 {

	public static void main(String[] args) {
		// 성적 처리 프로그램		
		// 난수를 이용해서 점수를 만듦 
		// 0 ~ 10 사이의 난수 5개를 저장할 수 있는 배열 선언
		// 배열에 난수 5개를 저장 -for문 5개. 이거빼곤 다 향상된 포문 가능
		// 배열에 저장된 모든 점수를 한 줄에 출력
		// 총점을 계산하고 출력: sum = 0 + scores[0] + scores[1] + scores[2] + ... -for문이나 향상된 for문
		// 평균을 계산하고 출력: (double)sum / length
		// 표준편차를 계산하고 출력: (s[0] - m)제곱 + (s[1] - m)제곱 + ... + (s[4] - m)제곱 / length ... 루트까지 
		// 							Math.sqrt() 메서드 이용 -> 루트

		// 최댓값을 찾아서 출력: max = scores[0]; 잡고 if (max < s[i]), max = s[i]. 원소끝까지 반복
//								max = scores[0];
//								for (첫번째 원소부터; 끝까지; 반복) {
//									if max < scores[i]
//								}
		// 최솟값을 찾아서 출력

		Random rand = new Random(); 
		int[] scores = new int[5];
		for (int r = 0; r < numbers.length; r++) {
		 	scores[i] = rand.nextInt()
		}
		
		
		
		
		
	}

}
